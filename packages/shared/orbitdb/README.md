# orbitdb

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orbitdb` to build the library.

## Running unit tests

Run `nx test orbitdb` to execute the unit tests via [Vitest](https://vitest.dev/).
