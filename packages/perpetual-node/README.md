# perpetual-node

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build perpetual-node` to build the library.

## Running unit tests

Run `nx test perpetual-node` to execute the unit tests via [Vitest](https://vitest.dev/).
