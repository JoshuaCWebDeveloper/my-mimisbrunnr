# orbitdb-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orbitdb-service` to build the library.

## Running unit tests

Run `nx test orbitdb-service` to execute the unit tests via [Vitest](https://vitest.dev/).
