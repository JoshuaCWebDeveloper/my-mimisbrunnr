# orbitdb-manager

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build orbitdb-manager` to build the library.

## Running unit tests

Run `nx test orbitdb-manager` to execute the unit tests via [Vitest](https://vitest.dev/).
