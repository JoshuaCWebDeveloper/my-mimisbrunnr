# validator-proxy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build validator-proxy` to build the library.

## Running unit tests

Run `nx test validator-proxy` to execute the unit tests via [Vitest](https://vitest.dev/).
